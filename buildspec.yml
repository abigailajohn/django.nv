version: 0.2
phases:
  install:
    runtime-versions:
      java: latest
  build:
    commands:
      - mvn verify sonar:sonar -Dsonar.projectKey=techstarrr_django -Dsonar.organization=techstarrr -Dsonar.host.url=https://sonarcloud.io -Dsonar.token=7a3b128155ae2c97798f5904901a77d011e9cb8e 
